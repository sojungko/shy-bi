const db = require('../database/config');

module.exports = {

  postImage({ secure_url }, callback) {
    console.log(`2) [bioModel.js/postImage] Accessing user database with url: ${secure_url}`
    );
  },
}
