const INITIAL_STATE = {
  minage: '',
  maxage: '',
  sex: '',
  city: 'New York',
};

export default function (state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
