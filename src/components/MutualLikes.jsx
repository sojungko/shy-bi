import React, { PropTypes } from 'react';
import UserListItem from './UserListItem';

const MutualLikes = (props) => (
  <div>
    <UserListItem />
  </div>
);

MutualLikes.propTypes = {
};

export default MutualLikes;
